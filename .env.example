# Copy to .env and fill in. Never commit the real .env.

# openai | groq | mock  (falls back to mock automatically if the selected
# provider's API key is unset)
LLM_PROVIDER=openai
OPENAI_API_KEY=
GROQ_API_KEY=
# defaults: gpt-4o-mini for openai, llama-3.3-70b-versatile for groq
LLM_MODEL=
