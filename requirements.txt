PyMuPDF>=1.24
scikit-learn>=1.3
numpy>=1.24
openai>=1.30
python-dotenv>=1.0
pytest>=7.4

# Optional, only needed for real scanned-PDF OCR (not installed in the
# reference environment this was built in -- see src/ingest/pdf_scanned.py):
# pytesseract>=0.3.10
# Pillow>=10.0

# Optional, only needed for a real DWG adapter (not implemented in this cut):
# ezdxf>=1.3
